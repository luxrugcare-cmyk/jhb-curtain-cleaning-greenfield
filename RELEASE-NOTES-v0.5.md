# v0.5

- Resend transactional domain provisioning initiated.
- Three draft transactional templates created in Resend.
- Resend lifecycle webhook created but deliberately disabled pending a live full-app endpoint.
- Application email adapter now prefers Resend template IDs via environment variables, retaining inline HTML fallback.
- Added clean-room GitHub repository handoff instructions.
- No production domain, CRM list, or customer data mutated.
