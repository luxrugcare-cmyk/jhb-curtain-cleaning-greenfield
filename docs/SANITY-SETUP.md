# Sanity CMS setup

The v0.3 codebase includes schemas and an optional client. No live Sanity project was created. Set `NEXT_PUBLIC_SANITY_PROJECT_ID`, dataset and API version to activate it. Until then the site can continue with code seed data.
